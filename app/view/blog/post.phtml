<div class="blogPost">
    <span class="title">{title}</span>
    <span class="posted">Updated: {static>date(Y-m-d H:i,modified)}</span>
    <span class="user">{username}</span>
    <span class="posted">Posted On: {static>date(Y-m-d H:i,created)}</span>
    <span class="body">{body}</span>
    <div class="clear">&nbsp;</div>
</div>